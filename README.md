Library makes sorting of list of objects with file name sorting specifics.

User's guide

Developer's guide
Library sorts generic lists including list with mixed object types. Out of the box ```List<String>``` and ```List<File>``` support provided.

